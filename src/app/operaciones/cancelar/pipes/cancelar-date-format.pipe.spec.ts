import { CancelarDateFormatPipe } from './cancelar-date-format.pipe';

describe('CancelarDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CancelarDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
